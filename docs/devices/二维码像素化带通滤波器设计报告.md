# 二维码像素化带通滤波器设计报告

Status: Draft
Domain: DEVICES
Canonical: `docs/devices/二维码像素化带通滤波器设计报告.md`
Related: `docs/flow/FLOW_STANDARD_PIPELINE_CONTRACT.md`, `docs/layout/LAYOUT_RECONSTRUCTION_CHECKLIST.md`, `docs/mfg/MFG_TOLERANCE_ROBUSTNESS_PLAN.md`, `docs/arch/ARCH_REFACTOR_TODO.md`
Last updated: 2026-08-01
Owner: ADS Automation

本文档用于建立 6-8 GHz 二维码式像素化带通滤波器分支。这里的“二维码滤波器”不是 ISO QR 编码器件，而是将耦合区域离散为 0/1 金属像素矩阵的微带滤波器。其本质属于 pixelated / binary-coded / inverse-designed microstrip filter。

## 1. 文献归纳

公开资料中更常见的关键词如下：

| 方向 | 可检索关键词 | 对本分支的意义 |
|---|---|---|
| 像素化滤波器 | `pixelated microwave filter`, `pixelated bandpass filter` | 将金属区域离散为小单元，通过二值拓扑改变耦合路径和寄生谐振。 |
| 二值编码结构 | `binary-coded microstrip`, `binary pixel resonator` | 适合用 0/1 参数表驱动版图生成和自动优化。 |
| 逆向设计 | `inverse-designed microstrip filter`, `direct binary search` | 可使用 DBS、GA、贝叶斯优化或代理模型逐步逼近目标 S 参数。 |
| QR-like 电磁结构 | `QR-code-like electromagnetic structure` | 图案外观类似二维码，但评价标准是 EM 响应，不是码字可读性。 |

文献共性结论：

- 像素化拓扑通常不能只靠闭式公式一次定型，需要全波 EM 仿真闭环。
- 像素开关会同时改变有效电长度、耦合强度、寄生零点和局部电流回流路径。
- 直接二值搜索适合早期探索，但变量数随矩阵阶数快速增加；8x8 已有 64 个二值变量，10x10 有 100 个二值变量。
- 对称约束可显著降低搜索空间，并让两端口结构更易保持互易和端口匹配。
- FR4 在 6-8 GHz 损耗和介电常数离散性较明显，因此最终必须做材料容差和加工容差复测。

本次检索到的关键参考：

| 资料 | 结论摘录 |
|---|---|
| Gomez et al., *Compact bandpass pixelated microwave filters with short-circuited stubs via inverse design*, Scientific Reports 2025, DOI: [`10.1038/s41598-025-10666-y`](https://doi.org/10.1038/s41598-025-10666-y) | 使用 DBS、GA 和随机变异算法驱动商业全波仿真，说明像素化图案需要仿真闭环；短路/接地相关结构可增强阻带响应。 |
| Zou, Song, Fan, *Ultracompact Planar Filter Design Based on Pixelated Configurations*, IEEE TMTT 2025, DOI: [`10.1109/TMTT.2024.3453506`](https://doi.org/10.1109/TMTT.2024.3453506) | 将上层金属定义为 M x N 二值矩阵，1 为金属、0 为开窗，并验证了低通和带通滤波器。 |
| Bi et al., *Frequency-Query Enhanced Electromagnetic Surrogate Modeling with Edge Anti-Aliasing Pixelation for Bandpass Filter Inverse Design*, IMS 2025, DOI: [`10.1109/ims40360.2025.11103901`](https://doi.org/10.1109/IMS40360.2025.11103901) | 使用像素化设计空间和代理模型预测多频点 S 参数，适合后续自学习优化路线。 |
| Wang et al., *A Generative Model-Based Method for Inverse Design of Microstrip Filters*, Electronics 2025, DOI: [`10.3390/electronics14101989`](https://doi.org/10.3390/electronics14101989) | 采用 GAN、CNN EM 响应预测器和 GA 优化器，将像素化图案与 S 参数目标关联。 |
| NEWCAS 2023 Paper 9037, *Pixelated RF: Random Metasurface Based Electromagnetic Filters* | 明确指出该类随机像素 RF surface 没有闭式解，可用 DBS 合成双频滤波器。 |

## 2. 设计目标

| 项目 | 初始值 |
|---|---|
| 材料 | FR4 |
| 介质厚度 | 0.210 mm |
| 铜厚 | 0.035 mm |
| 频段 | 4-10 GHz 仿真，6-8 GHz 通带 |
| 阻带目标 | 先按 5 GHz 约 20 dB 抑制探索 |
| 最小线宽/间距 | 初版按 4 mil，即 0.1016 mm |
| 端口规则 | P1/P2 位于 50 ohm 输入输出线端点，单位 mm |
| 图层规则 | `cond` 金属，`pcvia1` 过孔，`EM_BOUNDARY` 仿真边界 |

## 3. 初版拓扑

初版采用“二值像素耦合区 + 两侧 50 ohm 馈线”的形式：

```text
P1 feed -> taper/overlap -> binary pixel matrix -> taper/overlap -> P2 feed
```

矩阵规则：

- 8x8 和 10x10 两个尺寸同时生成。
- 像素单元按二维码模块方式铺设；相邻黑色像素共边贴合，形成连续铜皮，不使用额外补桥图形。
- 白色像素为空窗；非相邻铜岛之间的间距由一个或多个白色像素宽度决定。
- 默认使用左右镜像对称 mask，降低变量维度。
- 边缘行/列保留部分连续金属，保证输入输出有明确耦合路径。
- 孤立金属岛允许作为初始探索，但后续优化应记录 island 数量，并优先保留与主耦合路径相关的金属。

## 4. 初始参数表

| 参数 | 8x8 初值 | 10x10 初值 | 说明 |
|---|---:|---:|---|
| `matrix_n` | 8 | 10 | 像素矩阵阶数。 |
| `pixel_mm` | 0.50 | 0.42 | 单个金属像素边长。 |
| `gap_mm` | 0.00 | 0.00 | 连通模式下不在相邻像素之间插入白线；白色像素自身形成开窗间距。 |
| `region_w_mm` | 4.84 | 5.10 | `n*pixel+(n-1)*gap`。 |
| `region_h_mm` | 4.84 | 5.10 | 与宽度相同。 |
| `feed_w_mm` | 0.36 | 0.36 | FR4 210um L1 参考 50R 线宽的初始占位值，后续可用 ADS LineCalc 校准。 |
| `feed_len_mm` | 2.00 | 2.00 | DXF 导入后便于放端口和网格收敛。 |
| `coupling_overlap_mm` | 0.40 | 0.40 | 馈线伸入像素区域边缘的重叠量。 |
| `boundary_margin_mm` | 1.50 | 1.50 | EM 边界外扩。 |

## 5. 参数影响

| 参数 | 增大后的主要影响 | 优化方向 |
|---|---|---|
| 像素尺寸 | 等效电长度和电容加载增大，中心频率倾向降低。 | 如果通带偏高，可增大像素或增加连通路径。 |
| 白色像素开窗 | 金属连通路径减少，低频直通减弱，阻带零点更容易出现；过多开窗会导致耦合不足。 | 初期通过 mask 控制开窗分布，不再用补桥修正相邻黑色像素。 |
| 矩阵高度 | 横向寄生路径增多，零点和阻带形状更复杂。 | 8x8 用于快速探索，10x10 用于细调零点。 |
| 馈线重叠 | 输入输出耦合增强，回损和带宽会明显变化。 | 回损差或带宽窄时优先扫描该参数。 |
| 对称 mask | 响应更稳定，变量少。 | 初期保持对称，后期再开放局部非对称。 |
| 金属填充率 | 过高接近宽线/低通，过低耦合不足。 | 初始填充率建议 35%-60%。 |

## 6. 优化路线

推荐分三阶段：

| 阶段 | 方法 | 目标 |
|---|---|---|
| R0 | 生成规则 mask：checker、diag、qr_seed、edge_coupled、symmetric_random | 找到能形成 6-8 GHz 带通雏形的拓扑族。 |
| R1 | 固定 mask，扫描 feed overlap、pixel、gap、feed_w | 修正中心频率、带宽和端口匹配。 |
| R2 | 在最好 R1 附近做二值翻转 DBS/GA | 拉深 5 GHz 抑制，补充传输零点，降低通带插损。 |

评分建议：

```text
硬约束：min(S21 6-8GHz) > -6 dB，S21@5GHz < -20 dB。
软目标：提升通带最小 S21，降低通带纹波，改善 S11/S22。
惩罚项：违反最小间距、过多孤岛、端口未落在金属上、边界不足。
```

## 7. 可制造性

FR4 210um、4 mil 间距对 6-8 GHz 像素化结构可做初版验证，但需要注意：

- 像素间隙不要低于 0.1016 mm。
- 过多小孤岛会增加蚀刻偏差和 EM 网格敏感性，后续应统计孤岛数量。
- 50R 线宽必须用实际层叠重新校准；当前 0.36 mm 只是可生成初始值。
- FR4 的介电常数和损耗批次离散会带来中心频率和插损漂移，报告结论必须以 EMSETUP/FEM 结果为准。

## 8. 当前产物

已新增：

- `tools/layout/generate_pixel_qr_bpf_layout.py`
- `tools/generate_pixel_qr_bpf_layout.py`
- `config/projects/pixel_qr_bpf_fr4_210um.json`
- `config/pipelines/pixel_qr_bpf_fr4_210um_v1.json`
- `projects/pixel_qr_bpf_fr4_210um/plans/pixel_qr_bpf_fr4_210um_r0.csv`
- `projects/pixel_qr_bpf_fr4_210um/layouts/pixel_qr_bpf_fr4_210um_r0/`

该分支已经从 7 阶交指滤波器项目中拆分，后续配置、版图、结果和报告均在独立项目 `pixel_qr_bpf_fr4_210um` 下维护。

当前 R0 样例：

| 候选 | 矩阵 | 图案 | 像素/间距 | 总长 | 填充率 | DRC |
|---|---:|---|---|---:|---:|---|
| `pixel_qr8_fr4_210um_seed0_nogap` | 8x8 | `qr_seed` | 0.50 / 0.00 mm | 8.00 mm | 0.75 | PASS，最小分离间距 0.32 mm |
| `pixel_qr10_fr4_210um_seed1_nogap` | 10x10 | `symmetric_random` | 0.42 / 0.00 mm | 8.20 mm | 0.58 | PASS，最小分离间距 0.24 mm |

补桥式 R0 已停止作为正式候选。其单候选 FEM 结果显示结构更接近低通直通网络，5 GHz 抑制不足；后续优化应从无间隙像素网格出发，通过开窗分布、馈线耦合和接地/短路像素引入带通极点与阻带零点。
## 9. 当前阶段总结

二维码像素化带通滤波器分支已经完成从概念验证到 ADS/RFPro 自动仿真的初步闭环。当前分支采用 FR4、0.210 mm 介质厚度、35 um 铜厚、L1 参考地，目标频段为 6-8 GHz，仿真频段固定为 4-10 GHz。版图单位统一为 mm，金属层为 `cond`，过孔层为 `pcvia1`，边界层为 `EM_BOUNDARY`，ADS/RFPro setup 已固定使用 `6-8G_Fillter_lib:substrate4`，避免误用 RO4350 的 `substrate1`。当前结构不作为标准 QR 码电路处理，而是作为二值像素化微带滤波器处理。0/1 金属矩阵用于改变电流路径、局部耦合、电容加载和传输零点位置。该类结构没有可靠闭式公式，需要依赖全波仿真和参数/拓扑迭代。当前自动化链路已能完成参数表生成、DXF 版图导入、端口放置、EM setup 复制修补、RFPro FEM 仿真、S 参数导出和评分汇总。

## 10. 已验证参数与结果

当前正式进入 16x16 像素矩阵验证。R0/R1 采用 `qr_seed` 图案、左右镜像、边缘强制耦合、相邻黑色像素共边连通、10% overfill。4 mil 最小间距作为制造性硬约束。

| 候选 | 单元尺寸 | 制造性 | S21@5G | S21@6G | S21@7G | S21@8G | S21@9G | 结论 |
|---|---:|---|---:|---:|---:|---:|---:|---|
| `p022_ov10` | 0.22 mm | FAIL，最小分离间距 0.029 mm | - | - | - | - | - | 不送仿真，低于 4 mil 间距约束。 |
| `p025_ov10` | 0.25 mm | FAIL，最小分离间距 0.0575 mm | - | - | - | - | - | 不送仿真，低于 4 mil 间距约束。 |
| `p030_ov10` | 0.30 mm | PASS | -6.18 dB | -5.54 dB | -4.03 dB | -1.76 dB | -0.44 dB | 通带趋势已经形成，但高频侧过通，中心偏高。 |
| `p035_ov10` | 0.35 mm | PASS | -6.73 dB | -4.78 dB | -1.44 dB | -1.18 dB | -10.18 dB | 当前尺寸综合最好，6-8 GHz 更接近带通，高边开始收敛。 |
| `p040_ov10` | 0.40 mm | PASS | -5.85 dB | -1.78 dB | -1.54 dB | -10.71 dB | -13.23 dB | 单元偏大，8 GHz 已塌陷，通带被切窄。 |
| `p060_ov10` | 0.60 mm | PASS | -17.02 dB | -16.20 dB | -13.94 dB | -23.94 dB | -51.05 dB | 6-8 GHz 变成阻带，不适合作为当前 BPF 主方向。 |
| `p080_ov10` | 0.80 mm | PASS | -7.42 dB | -31.67 dB | -59.63 dB | -29.61 dB | -9.43 dB | 明显阻带化，排除。 |

综合结果表明，0.30-0.35 mm 单元尺寸是当前有效窗口。其中 0.30 mm 证明了结构可以形成 6-9 GHz 的传输抬升；0.35 mm 在 6-8 GHz 通带和 9 GHz 高边抑制之间更均衡；0.40 mm 开始造成通带高端塌陷。因此后续尺寸基准建议固定在 0.35 mm，并保留 0.32/0.33 mm 作为中心频率细调点。

## 11. 关键问题判断

当前方向是可行的，但尚未达到可用滤波器指标。主要限制不是版图无法形成带通，而是端口匹配和外部耦合仍然不足。已仿真的 0.30/0.35/0.40 mm 候选中，6-8 GHz 内最差 S11/S22 约为 -0.64 至 -1.93 dB，说明输入输出大部分能量仍被反射，滤波器尚未进入良好匹配状态。下一轮不应只继续扩大或缩小像素尺寸，而应优先调整馈线重叠、馈线宽度、输入输出过渡形状和边缘耦合像素分布。

从频率趋势看，增大像素尺寸会增加等效电长度和加载效应，使结构谐振/零点整体向低频移动；尺寸过大时 6-8 GHz 反而被切成阻带。0.35 mm 是当前比较合适的中间点。5 GHz 抑制仍未达到 20 dB，后续需要通过 mask 局部翻转、引入更强的低频零点路径，或增加接地/短路像素机制来改善低边阻带。

## 12. 后续 TODO

1. 完成 0.32 mm、0.33 mm、0.35 mm 三个尺寸点的细扫，确认中心频率和高边收敛趋势。
2. 在 0.35 mm 基准上扫描 `coupling_overlap_mm=0.45/0.55`，优先观察 S11/S22 是否改善。
3. 保持 4 mil 最小间距硬约束，低于 0.1016 mm 分离间距的候选不进入 RFPro 仿真。
4. 建立二维码分支专用评分表，除 S21 外增加回波损耗权重，避免只筛出“能传输但不匹配”的结构。
5. 在最佳尺寸和馈线耦合附近启动小规模二值翻转 DBS，每次只翻转少量像素，跟踪 5 GHz 抑制、6-8 GHz 插损、8-10 GHz 高边收敛和孤岛数量。
6. 后续若需要进一步提升 5 GHz 抑制，可评估短路像素、边缘接地过孔或局部 stub 化像素，但必须单独建立接地规则和过孔制造规则。
